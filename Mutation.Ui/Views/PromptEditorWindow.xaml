<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Mutation.Ui.Views.PromptEditorWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Mutation.Ui.Views.SettingsUi.Controls"
    mc:Ignorable="d"
    Title="Edit Prompt">

    <Grid Padding="24" RowSpacing="16" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Edit Prompt Configuration" Style="{StaticResource TitleTextBlockStyle}" />

        <Grid Grid.Row="1" ColumnSpacing="16" RowSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBox x:Name="TxtName"
                     Header="Name"
                     PlaceholderText="e.g. Summarize"
                     AutomationProperties.Name="Name"
                     AutomationProperties.HelpText="Name shown for this prompt in the prompt list."
                     Grid.Column="0" />
            <!-- The box and its clash warning move together, so the warning appearing
                 cannot push the Model row about. The warning is its own assertive live
                 region: it says the shortcut is already spoken for, and a user who cannot
                 see the badge has to hear it as it appears rather than on the way past. -->
            <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Bottom">
                <controls:HotkeyEditor x:Name="HkPromptHotkey"
                                       Header="Hotkey"
                                       HelpText="Optional global shortcut that runs this prompt."
                                       AllowEmpty="True" />
                <TextBlock x:Name="TxtHotkeyClash"
                           Text=""
                           Visibility="Collapsed"
                           TextWrapping="Wrap"
                           Foreground="OrangeRed"
                           Style="{StaticResource CaptionTextBlockStyle}"
                           AutomationProperties.LiveSetting="Assertive" />
            </StackPanel>

            <ComboBox x:Name="CmbModel"
                      Header="Model"
                      PlaceholderText="Select a model"
                      HorizontalAlignment="Stretch"
                      AutomationProperties.Name="Model"
                      AutomationProperties.HelpText="Language model used to run this prompt."
                      Grid.Row="1"
                      Grid.Column="0" />

            <!-- Declaration order is the tab order: Name, Hotkey, Model, Fast mode,
                 info hint, Auto run. The hint is itself a tab stop (by design — it has
                 to be reachable without a mouse), so it costs one Tab on the way to
                 Auto run; placing it after the check box keeps it next to what it
                 explains. Its tooltip and the check box's HelpText read from one
                 resource so the visual and screen-reader copy cannot drift apart. -->
            <StackPanel Orientation="Horizontal"
                        Spacing="6"
                        VerticalAlignment="Bottom"
                        Grid.Row="1"
                        Grid.Column="1">
                <CheckBox x:Name="ChkFastMode"
                          Content="Fast mode"
                          VerticalAlignment="Center"
                          AutomationProperties.Name="Fast mode"
                          AutomationProperties.HelpText="{StaticResource Help.Llm.FastMode}" />
                <controls:InfoHint Text="{StaticResource Help.Llm.FastMode}" VerticalAlignment="Center" />
            </StackPanel>

            <CheckBox x:Name="ChkAutoRun"
                      Content="Run automatically after transcription"
                      AutomationProperties.Name="Run automatically after transcription"
                      AutomationProperties.HelpText="Runs this prompt on the transcript as soon as a recording is transcribed, without a separate hotkey."
                      Grid.Row="2" Grid.ColumnSpan="2" />
        </Grid>

        <TextBox x:Name="TxtContent"
                 Grid.Row="2"
                 Header="Prompt Instructions"
                 PlaceholderText="Enter system instructions for the LLM..."
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 AutomationProperties.Name="Prompt instructions"
                 AutomationProperties.HelpText="System instructions sent to the language model ahead of the text being processed."
                 ScrollViewer.VerticalScrollBarVisibility="Auto" />

        <Grid Grid.Row="3" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- This window's only non-modal channel. A test run that is stopped, or a
                 repeat press on a stop already asked for, is said here rather than in a
                 dialog: an outcome the user asked for does not warrant stealing focus,
                 and silence from a button reads as one that did not register (#309). -->
            <InfoBar
                x:Name="StatusInfoBar"
                Grid.Column="0"
                Title="Status"
                IsOpen="False"
                IsClosable="True"
                CloseButtonClick="StatusInfoBar_CloseButtonClick"
                VerticalAlignment="Center"
                AutomationProperties.Name="Prompt editor status"
                AutomationProperties.LiveSetting="Polite"
                AutomationProperties.HelpText="Latest status message for this prompt" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
                <Button x:Name="BtnTest" Content="Test Run" Click="BtnTest_Click"
                        AutomationProperties.Name="Test run"
                        AutomationProperties.HelpText="Runs this prompt against the current clipboard content, using the settings above, without saving. Press it again while a run is going to stop it."
                        ToolTipService.ToolTip="Run the prompt against current clipboard content" />
                <Button x:Name="BtnSave" Content="Save" Style="{StaticResource AccentButtonStyle}" Click="BtnSave_Click" />
                <Button x:Name="BtnCancel" Content="Cancel" Click="BtnCancel_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
