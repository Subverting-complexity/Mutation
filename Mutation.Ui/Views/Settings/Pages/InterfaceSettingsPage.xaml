<?xml version="1.0" encoding="utf-8"?>
<UserControl
	x:Class="Mutation.Ui.Views.SettingsUi.Pages.InterfaceSettingsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:Mutation.Ui.Views.SettingsUi.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<StackPanel Spacing="16">
		<TextBlock Text="Interface" Style="{StaticResource SubtitleTextBlockStyle}" />

		<StackPanel Spacing="4">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<TextBlock Text="Max text-box visible lines" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
				<controls:InfoHint Text="{StaticResource Help.Interface.MaxVisibleLines}" VerticalAlignment="Center" />
			</StackPanel>
			<StackPanel Orientation="Horizontal" Spacing="8">
				<NumberBox x:Name="NbMaxLines"
						   AutomationProperties.Name="Max text-box visible lines"
						   AutomationProperties.HelpText="{StaticResource Help.Interface.MaxVisibleLines}"
						   Minimum="1"
						   Maximum="100"
						   SpinButtonPlacementMode="Inline"
						   SmallChange="1"
						   LargeChange="5"
						   Width="200"
						   ValueChanged="NbMaxLines_ValueChanged" />
				<Button x:Name="BtnResetMaxLines" Click="BtnResetMaxLines_Click"
						AutomationProperties.Name="Reset max visible lines">
					<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12"
							  AutomationProperties.AccessibilityView="Raw" />
				</Button>
			</StackPanel>
		</StackPanel>

		<StackPanel Spacing="4">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<TextBlock Text="Dictation insert preference" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
				<controls:InfoHint Text="{StaticResource Help.Interface.DictationInsert}" VerticalAlignment="Center" />
			</StackPanel>
			<StackPanel Orientation="Horizontal" Spacing="8">
				<ComboBox x:Name="CmbDictationInsert"
						  AutomationProperties.Name="Dictation insert preference"
						  AutomationProperties.HelpText="{StaticResource Help.Interface.DictationInsert}"
						  Width="220"
						  SelectionChanged="CmbDictationInsert_SelectionChanged" />
				<Button x:Name="BtnResetDictation" Click="BtnResetDictation_Click"
						AutomationProperties.Name="Reset dictation insert preference">
					<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12"
							  AutomationProperties.AccessibilityView="Raw" />
				</Button>
			</StackPanel>
		</StackPanel>

		<StackPanel Spacing="4">
			<TextBlock Text="Window position &amp; size" Style="{StaticResource BodyStrongTextBlockStyle}" />
			<Button x:Name="BtnResetWindow"
					Content="Reset window position"
					HorizontalAlignment="Left"
					Click="BtnResetWindow_Click" />
		</StackPanel>
	</StackPanel>
</UserControl>
