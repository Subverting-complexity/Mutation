<?xml version="1.0" encoding="utf-8"?>
<UserControl
	x:Class="Mutation.Ui.Views.SettingsUi.Pages.OcrSettingsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:Mutation.Ui.Views.SettingsUi.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<StackPanel Spacing="14">
		<TextBlock Text="Screen capture &amp; OCR" Style="{StaticResource SubtitleTextBlockStyle}" />

		<controls:SecretBox x:Name="TxtApiKey" Header="Azure Computer Vision API key" />

		<StackPanel Spacing="4">
			<TextBlock Text="Endpoint" Style="{StaticResource BodyStrongTextBlockStyle}" />
			<TextBox x:Name="TxtEndpoint" AutomationProperties.Name="Endpoint" PlaceholderText="https://..." TextChanged="TxtEndpoint_TextChanged" />
		</StackPanel>

		<StackPanel Orientation="Horizontal" Spacing="16">
			<StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<TextBlock Text="Timeout (seconds)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
					<controls:InfoHint Text="{StaticResource Help.Ocr.Timeout}" VerticalAlignment="Center" />
				</StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<NumberBox x:Name="NbTimeoutSeconds" AutomationProperties.Name="Timeout (seconds)" AutomationProperties.HelpText="{StaticResource Help.Ocr.Timeout}" Minimum="1" Maximum="600" SmallChange="1" LargeChange="5" Width="160" SpinButtonPlacementMode="Inline" ValueChanged="NbTimeoutSeconds_ValueChanged" />
					<Button Click="BtnResetTimeout_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset timeout">
						<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
					</Button>
				</StackPanel>
			</StackPanel>
			<StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<TextBlock Text="Free-tier page limit" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
					<controls:InfoHint Text="{StaticResource Help.Ocr.FreeTierPageLimit}" VerticalAlignment="Center" />
				</StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<NumberBox x:Name="NbFreeTierPageLimit" AutomationProperties.Name="Free-tier page limit" AutomationProperties.HelpText="{StaticResource Help.Ocr.FreeTierPageLimit}" Minimum="1" Maximum="20" SmallChange="1" Width="160" SpinButtonPlacementMode="Inline" ValueChanged="NbFreeTierPageLimit_ValueChanged" />
					<Button Click="BtnResetFreeTier_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset free-tier page limit">
						<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
					</Button>
				</StackPanel>
			</StackPanel>
		</StackPanel>

		<StackPanel Orientation="Horizontal" Spacing="16">
			<StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<TextBlock Text="Max parallel documents" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
					<controls:InfoHint Text="{StaticResource Help.Ocr.MaxParallelDocuments}" VerticalAlignment="Center" />
				</StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<NumberBox x:Name="NbMaxParallelDocuments" AutomationProperties.Name="Max parallel documents" AutomationProperties.HelpText="{StaticResource Help.Ocr.MaxParallelDocuments}" Minimum="1" Maximum="20" SmallChange="1" Width="180" SpinButtonPlacementMode="Inline" ValueChanged="NbMaxParallelDocuments_ValueChanged" />
					<Button Click="BtnResetMaxDocs_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset max parallel documents">
						<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
					</Button>
				</StackPanel>
			</StackPanel>
			<StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<TextBlock Text="Max parallel requests" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
					<controls:InfoHint Text="{StaticResource Help.Ocr.MaxParallelRequests}" VerticalAlignment="Center" />
				</StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<NumberBox x:Name="NbMaxParallelRequests" AutomationProperties.Name="Max parallel requests" AutomationProperties.HelpText="{StaticResource Help.Ocr.MaxParallelRequests}" Minimum="1" Maximum="20" SmallChange="1" Width="180" SpinButtonPlacementMode="Inline" ValueChanged="NbMaxParallelRequests_ValueChanged" />
					<Button Click="BtnResetMaxReqs_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset max parallel requests">
						<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
					</Button>
				</StackPanel>
			</StackPanel>
			<StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<TextBlock Text="Max document size (MB)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
					<controls:InfoHint Text="{StaticResource Help.Ocr.MaxDocumentSize}" VerticalAlignment="Center" />
				</StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<NumberBox x:Name="NbMaxDocumentSizeMb" AutomationProperties.Name="Max document size (MB)" AutomationProperties.HelpText="{StaticResource Help.Ocr.MaxDocumentSize}" Minimum="0" Maximum="500" SmallChange="1" LargeChange="10" Width="180" SpinButtonPlacementMode="Inline" ValueChanged="NbMaxDocumentSizeMb_ValueChanged" />
					<Button Click="BtnResetMaxDocSize_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset max document size">
						<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
					</Button>
				</StackPanel>
			</StackPanel>
		</StackPanel>

		<StackPanel Orientation="Horizontal" Spacing="6">
			<ToggleSwitch x:Name="ToggleUseFreeTier" Header="Use free tier" AutomationProperties.HelpText="{StaticResource Help.Ocr.UseFreeTier}" Toggled="ToggleUseFreeTier_Toggled" />
			<controls:InfoHint Text="{StaticResource Help.Ocr.UseFreeTier}" VerticalAlignment="Center" />
		</StackPanel>
		<StackPanel Orientation="Horizontal" Spacing="6">
			<ToggleSwitch x:Name="ToggleInvert" Header="Invert screenshot before OCR" AutomationProperties.HelpText="{StaticResource Help.Ocr.InvertScreenshot}" Toggled="ToggleInvert_Toggled" />
			<controls:InfoHint Text="{StaticResource Help.Ocr.InvertScreenshot}" VerticalAlignment="Center" />
		</StackPanel>

		<StackPanel Orientation="Horizontal" Spacing="6">
			<ToggleSwitch x:Name="TogglePasteOcrText" Header="Paste OCR text into the active app" AutomationProperties.HelpText="{StaticResource Help.Ocr.PasteIntoActiveApp}" Toggled="TogglePasteOcrText_Toggled" />
			<controls:InfoHint Text="{StaticResource Help.Ocr.PasteIntoActiveApp}" VerticalAlignment="Center" />
		</StackPanel>

		<StackPanel Spacing="8">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<ToggleSwitch x:Name="ToggleNudgePointer" Header="Wiggle the mouse pointer during a capture" AutomationProperties.HelpText="{StaticResource Help.Ocr.NudgePointer}" Toggled="ToggleNudgePointer_Toggled" />
				<controls:InfoHint Text="{StaticResource Help.Ocr.NudgePointer}" VerticalAlignment="Center" />
			</StackPanel>
			<StackPanel Orientation="Horizontal" Spacing="16">
				<StackPanel>
					<StackPanel Orientation="Horizontal" Spacing="6">
						<TextBlock Text="Wiggle every (milliseconds)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
						<controls:InfoHint Text="{StaticResource Help.Ocr.NudgeInterval}" VerticalAlignment="Center" />
					</StackPanel>
					<StackPanel Orientation="Horizontal" Spacing="6">
						<NumberBox x:Name="NbNudgeInterval" AutomationProperties.Name="Wiggle every (milliseconds)" AutomationProperties.HelpText="{StaticResource Help.Ocr.NudgeInterval}" Minimum="10" Maximum="500" SmallChange="10" LargeChange="50" Width="180" SpinButtonPlacementMode="Inline" ValueChanged="NbNudgeInterval_ValueChanged" />
						<Button Click="BtnResetNudgeInterval_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset wiggle interval">
							<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
						</Button>
					</StackPanel>
				</StackPanel>
				<StackPanel>
					<StackPanel Orientation="Horizontal" Spacing="6">
						<TextBlock Text="Keep wiggling for (milliseconds)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
						<controls:InfoHint Text="{StaticResource Help.Ocr.NudgeDuration}" VerticalAlignment="Center" />
					</StackPanel>
					<StackPanel Orientation="Horizontal" Spacing="6">
						<NumberBox x:Name="NbNudgeDuration" AutomationProperties.Name="Keep wiggling for (milliseconds)" AutomationProperties.HelpText="{StaticResource Help.Ocr.NudgeDuration}" Minimum="50" Maximum="5000" SmallChange="50" LargeChange="250" Width="200" SpinButtonPlacementMode="Inline" ValueChanged="NbNudgeDuration_ValueChanged" />
						<Button Click="BtnResetNudgeDuration_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset wiggle duration">
							<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
						</Button>
					</StackPanel>
				</StackPanel>
				<StackPanel>
					<StackPanel Orientation="Horizontal" Spacing="6">
						<TextBlock Text="Wiggle distance (pixels)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
						<controls:InfoHint Text="{StaticResource Help.Ocr.NudgeDistance}" VerticalAlignment="Center" />
					</StackPanel>
					<StackPanel Orientation="Horizontal" Spacing="6">
						<NumberBox x:Name="NbNudgeDistance" AutomationProperties.Name="Wiggle distance (pixels)" AutomationProperties.HelpText="{StaticResource Help.Ocr.NudgeDistance}" Minimum="1" Maximum="64" SmallChange="1" LargeChange="4" Width="180" SpinButtonPlacementMode="Inline" ValueChanged="NbNudgeDistance_ValueChanged" />
						<Button Click="BtnResetNudgeDistance_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset wiggle distance">
							<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
						</Button>
					</StackPanel>
				</StackPanel>
			</StackPanel>
		</StackPanel>

		<controls:HotkeyEditor x:Name="HkSendAfterOcr" Header="Send hotkey after OCR (optional)" HelpText="{StaticResource Help.Ocr.SendHotkeyAfter}" AllowEmpty="True" AllowSendKeysSyntax="True" />

		<TextBlock Text="OCR action hotkeys appear in the Hotkeys tab."
				   Style="{StaticResource CaptionTextBlockStyle}"
				   Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
	</StackPanel>
</UserControl>
