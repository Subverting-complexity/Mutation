<?xml version="1.0" encoding="utf-8"?>
<UserControl
	x:Class="Mutation.Ui.Views.SettingsUi.Pages.TtsSettingsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:Mutation.Ui.Views.SettingsUi.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<StackPanel Spacing="14">
		<TextBlock Text="Text to Speech" Style="{StaticResource SubtitleTextBlockStyle}" />

		<TextBlock Text="Voice, rate, and volume controls live on the main window. Other narration options:"
				   TextWrapping="Wrap"
				   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

		<StackPanel Orientation="Horizontal" Spacing="8">
			<ToggleSwitch x:Name="ToggleSpeechPreprocessing"
						  Header="Enable speech preprocessing"
						  AutomationProperties.HelpText="{StaticResource Help.Tts.Preprocessing}"
						  OffContent="Off"
						  OnContent="On"
						  Toggled="ToggleSpeechPreprocessing_Toggled" />
			<controls:InfoHint Text="{StaticResource Help.Tts.Preprocessing}" VerticalAlignment="Center" />
			<Button VerticalAlignment="Bottom" Click="BtnResetPreproc_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset speech preprocessing">
				<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
			</Button>
		</StackPanel>

		<StackPanel Spacing="4">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<TextBlock Text="Skip-back grace window (milliseconds)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
				<controls:InfoHint Text="{StaticResource Help.Tts.SkipSentenceGrace}" VerticalAlignment="Center" />
			</StackPanel>
			<StackPanel Orientation="Horizontal" Spacing="6">
				<NumberBox x:Name="NbSkipGrace"
						   AutomationProperties.Name="Skip-back grace window (milliseconds)"
						   AutomationProperties.HelpText="{StaticResource Help.Tts.SkipSentenceGrace}"
						   Minimum="250" Maximum="5000"
						   SmallChange="250" LargeChange="500"
						   SpinButtonPlacementMode="Inline"
						   Width="220"
						   ValueChanged="NbSkipGrace_ValueChanged" />
				<Button Click="BtnResetSkipGrace_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset skip-back grace window">
					<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
			</StackPanel>
		</StackPanel>

		<TextBlock Text="TTS hotkeys appear in the Hotkeys tab."
				   Style="{StaticResource CaptionTextBlockStyle}"
				   Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
	</StackPanel>
</UserControl>
