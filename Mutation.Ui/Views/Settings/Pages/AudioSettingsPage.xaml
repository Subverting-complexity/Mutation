<?xml version="1.0" encoding="utf-8"?>
<UserControl
	x:Class="Mutation.Ui.Views.SettingsUi.Pages.AudioSettingsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:Mutation.Ui.Views.SettingsUi.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<StackPanel Spacing="16">
		<TextBlock Text="Audio" Style="{StaticResource SubtitleTextBlockStyle}" />

		<StackPanel Orientation="Horizontal" Spacing="8">
			<ToggleSwitch x:Name="ToggleMicViz"
						  Header="Microphone visualization"
						  AutomationProperties.HelpText="{StaticResource Help.Audio.MicVisualization}"
						  OffContent="Off"
						  OnContent="On"
						  Toggled="ToggleMicViz_Toggled" />
			<controls:InfoHint Text="{StaticResource Help.Audio.MicVisualization}" VerticalAlignment="Center" />
			<Button VerticalAlignment="Bottom" Click="BtnResetMicViz_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset microphone visualization">
				<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
			</Button>
		</StackPanel>

		<controls:HotkeyEditor x:Name="HkMute" Header="Toggle microphone mute" />

		<StackPanel Spacing="8">
			<TextBlock Text="Custom beeps" Style="{StaticResource BodyStrongTextBlockStyle}" />
			<StackPanel Orientation="Horizontal" Spacing="6">
				<ToggleSwitch x:Name="ToggleUseCustomBeeps"
							  AutomationProperties.Name="Use custom beeps"
							  AutomationProperties.HelpText="{StaticResource Help.Audio.UseCustomBeeps}"
							  OffContent="Off"
							  OnContent="On"
							  Toggled="ToggleUseCustomBeeps_Toggled" />
				<controls:InfoHint Text="{StaticResource Help.Audio.UseCustomBeeps}" VerticalAlignment="Center" />
			</StackPanel>

			<Grid x:Name="BeepFilesPanel" RowSpacing="6" ColumnSpacing="8">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="120" />
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
					<RowDefinition Height="Auto" />
				</Grid.RowDefinitions>

				<TextBlock Text="Success" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" />
				<TextBox x:Name="TxtBeepSuccess" Grid.Row="0" Grid.Column="1" AutomationProperties.Name="Success beep file" TextChanged="BeepText_Changed" Tag="success" />
				<Button Grid.Row="0" Grid.Column="2" AutomationProperties.Name="Play success beep" ToolTipService.ToolTip="Play" Click="PlayBeep_Click" Tag="success">
					<FontIcon Glyph="&#xE768;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
				<Button Content="Browse..." Grid.Row="0" Grid.Column="3" AutomationProperties.Name="Browse for success beep file" Click="BrowseBeep_Click" Tag="success" />

				<TextBlock Text="Failure" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" />
				<TextBox x:Name="TxtBeepFailure" Grid.Row="1" Grid.Column="1" AutomationProperties.Name="Failure beep file" TextChanged="BeepText_Changed" Tag="failure" />
				<Button Grid.Row="1" Grid.Column="2" AutomationProperties.Name="Play failure beep" ToolTipService.ToolTip="Play" Click="PlayBeep_Click" Tag="failure">
					<FontIcon Glyph="&#xE768;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
				<Button Content="Browse..." Grid.Row="1" Grid.Column="3" AutomationProperties.Name="Browse for failure beep file" Click="BrowseBeep_Click" Tag="failure" />

				<TextBlock Text="Start" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0" />
				<TextBox x:Name="TxtBeepStart" Grid.Row="2" Grid.Column="1" AutomationProperties.Name="Start beep file" TextChanged="BeepText_Changed" Tag="start" />
				<Button Grid.Row="2" Grid.Column="2" AutomationProperties.Name="Play start beep" ToolTipService.ToolTip="Play" Click="PlayBeep_Click" Tag="start">
					<FontIcon Glyph="&#xE768;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
				<Button Content="Browse..." Grid.Row="2" Grid.Column="3" AutomationProperties.Name="Browse for start beep file" Click="BrowseBeep_Click" Tag="start" />

				<TextBlock Text="End" VerticalAlignment="Center" Grid.Row="3" Grid.Column="0" />
				<TextBox x:Name="TxtBeepEnd" Grid.Row="3" Grid.Column="1" AutomationProperties.Name="End beep file" TextChanged="BeepText_Changed" Tag="end" />
				<Button Grid.Row="3" Grid.Column="2" AutomationProperties.Name="Play end beep" ToolTipService.ToolTip="Play" Click="PlayBeep_Click" Tag="end">
					<FontIcon Glyph="&#xE768;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
				<Button Content="Browse..." Grid.Row="3" Grid.Column="3" AutomationProperties.Name="Browse for end beep file" Click="BrowseBeep_Click" Tag="end" />

				<TextBlock Text="Mute" VerticalAlignment="Center" Grid.Row="4" Grid.Column="0" />
				<TextBox x:Name="TxtBeepMute" Grid.Row="4" Grid.Column="1" AutomationProperties.Name="Mute beep file" TextChanged="BeepText_Changed" Tag="mute" />
				<Button Grid.Row="4" Grid.Column="2" AutomationProperties.Name="Play mute beep" ToolTipService.ToolTip="Play" Click="PlayBeep_Click" Tag="mute">
					<FontIcon Glyph="&#xE768;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
				<Button Content="Browse..." Grid.Row="4" Grid.Column="3" AutomationProperties.Name="Browse for mute beep file" Click="BrowseBeep_Click" Tag="mute" />

				<TextBlock Text="Unmute" VerticalAlignment="Center" Grid.Row="5" Grid.Column="0" />
				<TextBox x:Name="TxtBeepUnmute" Grid.Row="5" Grid.Column="1" AutomationProperties.Name="Unmute beep file" TextChanged="BeepText_Changed" Tag="unmute" />
				<Button Grid.Row="5" Grid.Column="2" AutomationProperties.Name="Play unmute beep" ToolTipService.ToolTip="Play" Click="PlayBeep_Click" Tag="unmute">
					<FontIcon Glyph="&#xE768;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
				<Button Content="Browse..." Grid.Row="5" Grid.Column="3" AutomationProperties.Name="Browse for unmute beep file" Click="BrowseBeep_Click" Tag="unmute" />
			</Grid>

			<InfoBar x:Name="BeepPreviewInfoBar"
					 IsOpen="False"
					 IsClosable="True"
					 Severity="Error"
					 Title="Beep preview" />
		</StackPanel>
	</StackPanel>
</UserControl>
