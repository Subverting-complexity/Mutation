<?xml version="1.0" encoding="utf-8"?>
<UserControl
	x:Class="Mutation.Ui.Views.SettingsUi.Pages.SpeechSettingsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:Mutation.Ui.Views.SettingsUi.Controls"
	xmlns:local="using:Mutation.Ui"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<StackPanel Spacing="14">
		<TextBlock Text="Speech to Text" Style="{StaticResource SubtitleTextBlockStyle}" />

		<StackPanel Spacing="8">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<TextBlock Text="Service definitions" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
				<controls:InfoHint Text="{StaticResource Help.Speech.Services}" VerticalAlignment="Center" />
			</StackPanel>
			<TextBlock Text="Changes to a service definition take effect after you restart the app; the per-service prompt applies immediately. Choose which service is active from the main window."
					   Style="{StaticResource CaptionTextBlockStyle}"
					   TextWrapping="Wrap"
					   Foreground="{ThemeResource TextFillColorTertiaryBrush}" />

			<ItemsControl x:Name="ServicesList"
						  AutomationProperties.Name="Speech to text service definitions"
						  ItemsSource="{x:Bind ServiceEntries, Mode=OneWay}">
				<ItemsControl.ItemsPanel>
					<ItemsPanelTemplate>
						<StackPanel Spacing="8" />
					</ItemsPanelTemplate>
				</ItemsControl.ItemsPanel>
				<ItemsControl.ItemTemplate>
					<DataTemplate x:DataType="local:SpeechServiceEntry">
						<Expander HorizontalAlignment="Stretch"
								  HorizontalContentAlignment="Stretch"
								  Header="{x:Bind HeaderText, Mode=OneWay}"
								  AutomationProperties.Name="{x:Bind HeaderText, Mode=OneWay}">
							<StackPanel Spacing="10" Margin="0,4,0,4">
								<StackPanel Spacing="4">
									<StackPanel Orientation="Horizontal" Spacing="6">
										<TextBlock Text="Name" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
										<controls:InfoHint Text="{StaticResource Help.Speech.Service.Name}" VerticalAlignment="Center" />
									</StackPanel>
									<TextBox Text="{x:Bind Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
											 AutomationProperties.Name="Service name"
											 AutomationProperties.HelpText="{StaticResource Help.Speech.Service.Name}" />
								</StackPanel>

								<StackPanel Spacing="4">
									<StackPanel Orientation="Horizontal" Spacing="6">
										<TextBlock Text="Provider" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
										<controls:InfoHint Text="{StaticResource Help.Speech.Service.Provider}" VerticalAlignment="Center" />
									</StackPanel>
									<ComboBox ItemsSource="{x:Bind ProviderOptions}"
											  SelectedItem="{x:Bind Provider, Mode=TwoWay}"
											  Width="280"
											  HorizontalAlignment="Left"
											  AutomationProperties.Name="Provider"
											  AutomationProperties.HelpText="{StaticResource Help.Speech.Service.Provider}" />
								</StackPanel>

								<controls:SecretBox Header="API key override (optional)"
													HelpText="{StaticResource Help.Speech.Service.ApiKey}"
													Secret="{x:Bind ApiKey, Mode=TwoWay}" />

								<StackPanel Spacing="4">
									<StackPanel Orientation="Horizontal" Spacing="6">
										<TextBlock Text="Base domain" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
										<controls:InfoHint Text="{StaticResource Help.Speech.Service.BaseDomain}" VerticalAlignment="Center" />
									</StackPanel>
									<TextBox Text="{x:Bind BaseDomain, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
											 AutomationProperties.Name="Base domain"
											 AutomationProperties.HelpText="{StaticResource Help.Speech.Service.BaseDomain}" />
								</StackPanel>

								<StackPanel Spacing="4">
									<StackPanel Orientation="Horizontal" Spacing="6">
										<TextBlock Text="Model id" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
										<controls:InfoHint Text="{StaticResource Help.Speech.Service.ModelId}" VerticalAlignment="Center" />
									</StackPanel>
									<TextBox Text="{x:Bind ModelId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
											 AutomationProperties.Name="Model id"
											 AutomationProperties.HelpText="{StaticResource Help.Speech.Service.ModelId}" />
								</StackPanel>

								<StackPanel Spacing="4">
									<StackPanel Orientation="Horizontal" Spacing="6">
										<TextBlock Text="Prompt" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
										<controls:InfoHint Text="{StaticResource Help.Speech.Service.Prompt}" VerticalAlignment="Center" />
									</StackPanel>
									<TextBox Text="{x:Bind SpeechToTextPrompt, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
											 AcceptsReturn="True"
											 TextWrapping="Wrap"
											 MinHeight="60"
											 AutomationProperties.Name="Per-service prompt"
											 AutomationProperties.HelpText="{StaticResource Help.Speech.Service.Prompt}" />
								</StackPanel>

								<StackPanel Spacing="4">
									<StackPanel Orientation="Horizontal" Spacing="6">
										<TextBlock Text="Timeout (seconds)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
										<controls:InfoHint Text="{StaticResource Help.Speech.Service.Timeout}" VerticalAlignment="Center" />
									</StackPanel>
									<NumberBox Value="{x:Bind TimeoutSeconds, Mode=TwoWay}"
											   Minimum="1" Maximum="600"
											   SmallChange="1" LargeChange="10"
											   SpinButtonPlacementMode="Inline"
											   Width="180"
											   HorizontalAlignment="Left"
											   AutomationProperties.Name="Service timeout (seconds)"
											   AutomationProperties.HelpText="{StaticResource Help.Speech.Service.Timeout}" />
								</StackPanel>

								<Button Style="{StaticResource SubtleButtonStyle}"
										Content="Delete service"
										AutomationProperties.Name="Delete service"
										Click="ServiceDelete_Click"
										Tag="{x:Bind}" />
							</StackPanel>
						</Expander>
					</DataTemplate>
				</ItemsControl.ItemTemplate>
			</ItemsControl>

			<Button x:Name="BtnAddService"
					Style="{StaticResource AccentButtonStyle}"
					HorizontalAlignment="Left"
					Click="BtnAddService_Click"
					Content="Add service" />
		</StackPanel>

		<StackPanel Orientation="Horizontal" Spacing="16">
			<StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<TextBlock Text="File transcription timeout (seconds)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
					<controls:InfoHint Text="{StaticResource Help.Speech.FileTimeout}" VerticalAlignment="Center" />
				</StackPanel>
				<StackPanel Orientation="Horizontal" Spacing="6">
					<NumberBox x:Name="NbFileTimeout"
							   AutomationProperties.Name="File transcription timeout (seconds)"
							   AutomationProperties.HelpText="{StaticResource Help.Speech.FileTimeout}"
							   Minimum="10" Maximum="7200"
							   SmallChange="10" LargeChange="60"
							   SpinButtonPlacementMode="Inline"
							   Width="220"
							   ValueChanged="NbFileTimeout_ValueChanged" />
					<Button Click="BtnResetFileTimeout_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset file transcription timeout">
						<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
					</Button>
				</StackPanel>
			</StackPanel>
		</StackPanel>

		<StackPanel Spacing="4">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<TextBlock Text="Recording sessions to keep" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
				<controls:InfoHint Text="{StaticResource Help.Speech.MaxRetainedSessions}" VerticalAlignment="Center" />
			</StackPanel>
			<StackPanel Orientation="Horizontal" Spacing="6">
				<NumberBox x:Name="NbMaxRetainedSessions"
						   AutomationProperties.Name="Recording sessions to keep"
						   AutomationProperties.HelpText="{StaticResource Help.Speech.MaxRetainedSessions}"
						   ToolTipService.ToolTip="{StaticResource Help.Speech.MaxRetainedSessions}"
						   Minimum="1" Maximum="500"
						   SmallChange="1" LargeChange="10"
						   SpinButtonPlacementMode="Inline"
						   Width="220"
						   ValueChanged="NbMaxRetainedSessions_ValueChanged" />
				<Button Click="BtnResetMaxRetainedSessions_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset recording sessions to keep">
					<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
			</StackPanel>
		</StackPanel>

		<StackPanel Spacing="4">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<TextBlock Text="Temp directory" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
				<controls:InfoHint Text="{StaticResource Help.Speech.TempDir}" VerticalAlignment="Center" />
			</StackPanel>
			<Grid ColumnSpacing="8">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>
				<TextBox x:Name="TxtTempDir" Grid.Column="0" AutomationProperties.Name="Temp directory" AutomationProperties.HelpText="{StaticResource Help.Speech.TempDir}" TextChanged="TxtTempDir_TextChanged" />
				<Button x:Name="BtnBrowseTempDir" Grid.Column="1" Content="Browse..." AutomationProperties.Name="Browse for temp directory" Click="BtnBrowseTempDir_Click" />
				<Button Grid.Column="2" Click="BtnResetTempDir_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset temp directory">
					<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
			</Grid>
		</StackPanel>

		<controls:HotkeyEditor x:Name="HkSendAfterTranscription"
							   Header="Send hotkey after transcription (optional)"
							   HelpText="{StaticResource Help.Speech.SendHotkeyAfter}"
							   AllowEmpty="True"
							   AllowSendKeysSyntax="True" />

		<TextBlock Text="Silence stripping" Style="{StaticResource SubtitleTextBlockStyle}" />

		<StackPanel Spacing="4">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<CheckBox x:Name="ChkStripSilence"
						  Content="Strip silent gaps from audio"
						  AutomationProperties.Name="Strip silent gaps from audio"
						  AutomationProperties.HelpText="{StaticResource Help.Speech.StripSilence}"
						  Checked="ChkStripSilence_Changed"
						  Unchecked="ChkStripSilence_Changed" />
				<controls:InfoHint Text="{StaticResource Help.Speech.StripSilence}" VerticalAlignment="Center" />
			</StackPanel>
		</StackPanel>

		<StackPanel Spacing="4">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<TextBlock Text="Minimum silence to keep (seconds)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
				<controls:InfoHint Text="{StaticResource Help.Speech.MinSilence}" VerticalAlignment="Center" />
			</StackPanel>
			<StackPanel Orientation="Horizontal" Spacing="6">
				<NumberBox x:Name="NbMinSilence"
						   AutomationProperties.Name="Minimum silence to keep (seconds)"
						   AutomationProperties.HelpText="{StaticResource Help.Speech.MinSilence}"
						   Minimum="0" Maximum="5"
						   SmallChange="0.1" LargeChange="0.5"
						   SpinButtonPlacementMode="Inline"
						   Width="220"
						   ValueChanged="NbMinSilence_ValueChanged" />
				<Button Click="BtnResetMinSilence_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset minimum silence to keep">
					<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
			</StackPanel>
		</StackPanel>

		<StackPanel Spacing="4">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<TextBlock Text="Silence threshold (dBFS)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
				<controls:InfoHint Text="{StaticResource Help.Speech.SilenceThreshold}" VerticalAlignment="Center" />
			</StackPanel>
			<StackPanel Orientation="Horizontal" Spacing="6">
				<NumberBox x:Name="NbSilenceThreshold"
						   AutomationProperties.Name="Silence threshold (dBFS)"
						   AutomationProperties.HelpText="{StaticResource Help.Speech.SilenceThreshold}"
						   Minimum="-60" Maximum="-20"
						   SmallChange="1" LargeChange="5"
						   SpinButtonPlacementMode="Inline"
						   Width="220"
						   ValueChanged="NbSilenceThreshold_ValueChanged" />
				<Button Click="BtnResetSilenceThreshold_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset silence threshold">
					<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
			</StackPanel>
		</StackPanel>

		<StackPanel Spacing="4">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<TextBlock Text="Edge guard (milliseconds)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
				<controls:InfoHint Text="{StaticResource Help.Speech.SilenceGuard}" VerticalAlignment="Center" />
			</StackPanel>
			<StackPanel Orientation="Horizontal" Spacing="6">
				<NumberBox x:Name="NbSilenceGuard"
						   AutomationProperties.Name="Edge guard (milliseconds)"
						   AutomationProperties.HelpText="{StaticResource Help.Speech.SilenceGuard}"
						   Minimum="0" Maximum="500"
						   SmallChange="10" LargeChange="50"
						   SpinButtonPlacementMode="Inline"
						   Width="220"
						   ValueChanged="NbSilenceGuard_ValueChanged" />
				<Button Click="BtnResetSilenceGuard_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset edge guard">
					<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
			</StackPanel>
		</StackPanel>

		<TextBlock Text="Large recordings" Style="{StaticResource SubtitleTextBlockStyle}" />

		<StackPanel Spacing="4">
			<StackPanel Orientation="Horizontal" Spacing="6">
				<TextBlock Text="Maximum upload size (MB)" VerticalAlignment="Center" Style="{StaticResource BodyStrongTextBlockStyle}" />
				<controls:InfoHint Text="{StaticResource Help.Speech.MaxUploadSize}" VerticalAlignment="Center" />
			</StackPanel>
			<StackPanel Orientation="Horizontal" Spacing="6">
				<NumberBox x:Name="NbMaxUploadSizeMb"
						   AutomationProperties.Name="Maximum upload size (MB)"
						   AutomationProperties.HelpText="{StaticResource Help.Speech.MaxUploadSize}"
						   Minimum="0" Maximum="1000"
						   SmallChange="1" LargeChange="10"
						   SpinButtonPlacementMode="Inline"
						   Width="220"
						   ValueChanged="NbMaxUploadSizeMb_ValueChanged" />
				<Button Click="BtnResetMaxUploadSize_Click" ToolTipService.ToolTip="Reset to default" AutomationProperties.Name="Reset maximum upload size">
					<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" AutomationProperties.AccessibilityView="Raw" />
				</Button>
			</StackPanel>
		</StackPanel>
	</StackPanel>
</UserControl>
