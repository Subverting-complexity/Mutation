<?xml version="1.0" encoding="utf-8"?>
<UserControl
	x:Class="Mutation.Ui.Views.SettingsUi.Pages.SpeechSettingsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:Mutation.Ui.Views.SettingsUi.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<StackPanel Spacing="14">
		<TextBlock Text="Speech to Text" Style="{StaticResource SubtitleTextBlockStyle}" />

		<StackPanel Spacing="4">
			<TextBlock Text="Active service" Style="{StaticResource BodyStrongTextBlockStyle}" />
			<ComboBox x:Name="CmbActiveService"
					  Width="280"
					  HorizontalAlignment="Left"
					  SelectionChanged="CmbActiveService_SelectionChanged" />
			<TextBlock Text="(Edit individual service definitions on the main window or in Mutation.json.)"
					   Style="{StaticResource CaptionTextBlockStyle}"
					   Foreground="{ThemeResource TextFillColorTertiaryBrush}" />
		</StackPanel>

		<StackPanel Orientation="Horizontal" Spacing="16">
			<StackPanel>
				<TextBlock Text="File transcription timeout (seconds)" Style="{StaticResource BodyStrongTextBlockStyle}" />
				<StackPanel Orientation="Horizontal" Spacing="6">
					<NumberBox x:Name="NbFileTimeout"
							   Minimum="10" Maximum="7200"
							   SmallChange="10" LargeChange="60"
							   SpinButtonPlacementMode="Inline"
							   Width="220"
							   ValueChanged="NbFileTimeout_ValueChanged" />
					<Button Click="BtnResetFileTimeout_Click" ToolTipService.ToolTip="Reset to default">
						<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" />
					</Button>
				</StackPanel>
			</StackPanel>
		</StackPanel>

		<StackPanel Spacing="4">
			<TextBlock Text="Temp directory" Style="{StaticResource BodyStrongTextBlockStyle}" />
			<Grid ColumnSpacing="8">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="Auto" />
				</Grid.ColumnDefinitions>
				<TextBox x:Name="TxtTempDir" Grid.Column="0" TextChanged="TxtTempDir_TextChanged" />
				<Button x:Name="BtnBrowseTempDir" Grid.Column="1" Content="Browse..." Click="BtnBrowseTempDir_Click" />
				<Button Grid.Column="2" Click="BtnResetTempDir_Click" ToolTipService.ToolTip="Reset to default">
					<FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" FontSize="12" />
				</Button>
			</Grid>
		</StackPanel>

		<controls:HotkeyEditor x:Name="HkSendAfterTranscription"
							   Header="Send hotkey after transcription (optional)"
							   AllowEmpty="True"
							   AllowSendKeysSyntax="True" />
	</StackPanel>
</UserControl>
