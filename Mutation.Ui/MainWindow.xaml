<?xml version="1.0" encoding="utf-8"?>
<!-- Settings hamburger menu implemented -->
<Window
    x:Class="Mutation.Ui.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mutation.Ui"
    xmlns:ScottPlot="using:ScottPlot.WinUI"
    xmlns:controls="using:Mutation.Ui.Views.SettingsUi.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:Mutation.Ui.Converters"
    mc:Ignorable="d"
    Title="Mutation">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Background="{ThemeResource AppBackgroundBrush}">
        <Grid.Resources>
            <converters:BooleanToVisibilityConverter x:Key="BoolToVisConverter" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="6*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Hamburger Menu Button (Top Left) -->
            <Button
                x:Name="HamburgerButton"
                Grid.Column="0"
                Style="{StaticResource IconButtonStyle}"
                Margin="16,24,0,12"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                AccessKey="H"
                AutomationProperties.Name="Menu"
                ToolTipService.ToolTip="Menu (Alt, H)">
                <Button.Flyout>
                    <MenuFlyout Placement="BottomEdgeAlignedLeft">
                        <MenuFlyoutItem
                            x:Name="SettingsMenuItem"
                            Text="Settings"
                            KeyboardAcceleratorTextOverride="Ctrl+,"
                            Click="SettingsMenuItem_Click">
                            <MenuFlyoutItem.Icon>
                                <FontIcon Glyph="&#xE713;"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator />
                        <MenuFlyoutSubItem Text="Debug">
                            <MenuFlyoutSubItem.Icon>
                                <FontIcon Glyph="&#xEBE8;"/>
                            </MenuFlyoutSubItem.Icon>
                            <MenuFlyoutItem 
                                Text="Simulate UI Crash" 
                                Click="DebugSimulateUiCrash_Click"
                                ToolTipService.ToolTip="Throws an unhandled exception on the UI thread"/>
                            <MenuFlyoutItem 
                                Text="Simulate Background Crash" 
                                Click="DebugSimulateBackgroundCrash_Click"
                                ToolTipService.ToolTip="Throws an unhandled exception on a background thread"/>
                            <MenuFlyoutItem 
                                Text="Simulate Task Crash" 
                                Click="DebugSimulateTaskCrash_Click"
                                ToolTipService.ToolTip="Throws an unobserved exception in an async Task"/>
                        </MenuFlyoutSubItem>
                    </MenuFlyout>
                </Button.Flyout>
                <FontIcon Glyph="&#xE700;" FontFamily="Segoe Fluent Icons"/>
            </Button>

            <StackPanel Grid.Column="1" Margin="32,24,32,12" Spacing="4">
                <TextBlock Text="Mutation Workspace" Style="{StaticResource TitleTextStyle}" AutomationProperties.Name="Application Title" />
                <TextBlock Text="Capture ideas, format transcripts, and automate workflows from one polished surface." Style="{StaticResource SubtitleTextStyle}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="6*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ScrollViewer
                Grid.Column="1"
                Padding="32,0,32,32"
                HorizontalAlignment="Stretch"
                HorizontalScrollBarVisibility="Disabled"
                VerticalScrollBarVisibility="Auto"
                VerticalScrollMode="Auto">
                <StackPanel x:Name="ContentStack" Spacing="20">
                    <Grid x:Name="ContentGrid" ColumnSpacing="18" RowSpacing="18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="PrimaryColumn" Width="*" />
                            <ColumnDefinition x:Name="SecondaryColumn" Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Grid.Transitions>
                            <TransitionCollection>
                                <EntranceThemeTransition FromHorizontalOffset="48" />
                            </TransitionCollection>
                        </Grid.Transitions>

                    <!-- Microphone Management -->
                    <Border x:Name="MicrophoneCard" Style="{StaticResource CardStyle}" Grid.Row="0" Grid.Column="0">
                        <Grid>
                            <Rectangle x:Name="MicPulseOverlay" Fill="{ThemeResource PrimaryBrush}" Opacity="0" IsHitTestVisible="False" RadiusX="16" RadiusY="16" />
                            <StackPanel Spacing="0">
                            <StackPanel Spacing="4" Margin="0,0,0,16">
                                <TextBlock Text="Microphone" Style="{StaticResource SectionHeaderTextStyle}" />
                            </StackPanel>

                            <Grid ColumnSpacing="16" RowSpacing="12" Margin="0,0,0,10">
                                <StackPanel Spacing="6">
                                    <Grid ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Grid Width="28" Height="28" VerticalAlignment="Center">
                                            <FontIcon
                                                x:Name="MicStatusIcon"
                                                Glyph="&#xE720;"
                                                FontFamily="Segoe Fluent Icons"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center" />
                                            <Rectangle
                                                x:Name="MicStatusIconSlash"
                                                Width="20"
                                                Height="2"
                                                RadiusX="1"
                                                RadiusY="1"
                                                Fill="{Binding Foreground, ElementName=MicStatusIcon}"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Visibility="Collapsed"
                                                IsHitTestVisible="False"
                                                RenderTransformOrigin="0.5,0.5">
                                                <Rectangle.RenderTransform>
                                                    <CompositeTransform Rotation="-45" />
                                                </Rectangle.RenderTransform>
                                            </Rectangle>
                                        </Grid>
                                        <ComboBox
                                            x:Name="CmbMicrophone"
                                            Grid.Column="1"
                                            PlaceholderText="Select a microphone"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Center"
                                            AutomationProperties.Name="Microphone selection"
                                            ToolTipService.ToolTip="Choose the input microphone"
                                            SelectionChanged="CmbMicrophone_SelectionChanged" />
                                        <Button
                                            x:Name="BtnToggleMic"
                                            Grid.Column="2"
                                            Style="{StaticResource AccentButtonStyle}"
                                            VerticalAlignment="Center"
                                            Click="BtnToggleMic_Click"
                                            AccessKey="M"
                                            AutomationProperties.Name="Toggle microphone"
                                            ToolTipService.ToolTip="Toggle microphone mute state (Alt+Q)">
                                            <Grid Width="28" Height="28">
                                                <FontIcon
                                                    x:Name="BtnToggleMicIcon"
                                                    Glyph="&#xE720;"
                                                    FontFamily="Segoe Fluent Icons"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                />
                                                <Rectangle
                                                    x:Name="BtnToggleMicSlash"
                                                    Width="20"
                                                    Height="2"
                                                    RadiusX="1"
                                                    RadiusY="1"
                                                    Fill="{Binding Foreground, ElementName=BtnToggleMicIcon}"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Visibility="Collapsed"
                                                    IsHitTestVisible="False"
                                                    RenderTransformOrigin="0.5,0.5">
                                                    <Rectangle.RenderTransform>
                                                        <CompositeTransform Rotation="-45" />
                                                    </Rectangle.RenderTransform>
                                                </Rectangle>
                                            </Grid>
                                        </Button>
                                    </Grid>
                                </StackPanel>
                            </Grid>

                            <Grid x:Name="MicWaveContainer">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Grid x:Name="MicWaveArea" Grid.Column="0" PointerReleased="MicWaveArea_PointerReleased">
                                    <ScottPlot:WinUIPlot
                                        x:Name="MicWaveformPlot"
                                        Height="120"
                                        IsHitTestVisible="False" />
                                    <TextBlock x:Name="MicWaveformOffLabel" Text="Off" FontSize="32" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed" />
                                </Grid>
                                <StackPanel Grid.Column="1" Margin="12,0,0,0" Spacing="6" VerticalAlignment="Stretch">
                                    <Grid
                                        Width="16"
                                        Height="120"
                                        Background="{ThemeResource ControlFillColorDefaultBrush}"
                                        CornerRadius="8"
                                        x:Name="MicLevelMeter">
                                        <Rectangle
                                            x:Name="RmsLevelBar"
                                            Width="16"
                                            Height="0"
                                            Fill="{ThemeResource PrimaryBrush}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Bottom"
                                            RadiusX="8"
                                            RadiusY="8" />
                                    </Grid>
                                </StackPanel>
                            </Grid>

                            <StackPanel x:Name="MicLevelPinPanel" Spacing="8" Margin="0,12,0,0">
                                <ToggleSwitch
                                    x:Name="TglPinMicLevel"
                                    Header="Pin input level"
                                    OffContent="Off"
                                    OnContent="On"
                                    Toggled="TglPinMicLevel_Toggled"
                                    AutomationProperties.Name="Pin microphone input level"
                                    ToolTipService.ToolTip="Keep the Windows capture level fixed at the chosen value, re-asserting it on each recording, mic change, and app start" />
                                <StackPanel Spacing="4">
                                    <TextBlock Text="Input level" />
                                    <Slider
                                        x:Name="SldMicLevel"
                                        Minimum="0"
                                        Maximum="100"
                                        StepFrequency="1"
                                        TickFrequency="10"
                                        TickPlacement="Outside"
                                        SnapsTo="StepValues"
                                        ValueChanged="SldMicLevel_ValueChanged"
                                        AutomationProperties.Name="Microphone input level"
                                        ToolTipService.ToolTip="Set the Windows capture level (0 to 100); the change applies immediately, even when not recording" />
                                </StackPanel>
                            </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Speech to Text -->
                    <Border x:Name="SpeechCard" Style="{StaticResource CardStyle}" Grid.Row="0" Grid.Column="1">
                        <StackPanel Spacing="18">
                            <StackPanel Spacing="4">
                                <TextBlock Text="Speech to Text" Style="{StaticResource SectionHeaderTextStyle}" />
                            </StackPanel>

                            <Grid ColumnSpacing="16" RowSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Column="0" Grid.Row="0" Spacing="6" VerticalAlignment="Center">
                                    <ComboBox
                                        x:Name="CmbSpeechService"
                                        PlaceholderText="Select a speech-to-text service"
                                        AutomationProperties.Name="Speech-to-text service selection"
                                        ToolTipService.ToolTip="Pick the transcription engine"
                                        SelectionChanged="CmbSpeechService_SelectionChanged" />
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <Button
                                        x:Name="BtnSpeechToText"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Click="BtnSpeechToText_Click"
                                        AccessKey="R"
                                        AutomationProperties.Name="Record"
                                        AutomationProperties.HelpText="{StaticResource Help.Main.Record}"
                                        ToolTipService.ToolTip="Start or stop speech capture (Shift+Alt+U). The transcript appears in the Raw Transcript box.">
                                        <FontIcon
                                            x:Name="BtnSpeechToTextIcon"
                                            Glyph="&#xE7C8;"
                                            FontFamily="Segoe Fluent Icons"
                                        />
                                    </Button>
                                    <Button
                                        x:Name="BtnSpeechToTextWithFormat"
                                        Style="{StaticResource AccentButtonStyle}"
                                        Click="BtnSpeechToTextWithFormat_Click"
                                        AutomationProperties.Name="Record and Format"
                                        AutomationProperties.HelpText="{StaticResource Help.Main.RecordAndFormat}"
                                        ToolTipService.ToolTip="Start or stop speech capture, then automatically process the transcript with the LLM.">
                                        <FontIcon
                                            x:Name="BtnSpeechToTextWithFormatIcon"
                                            Glyph="&#xE890;"
                                            FontFamily="Segoe Fluent Icons"
                                        />
                                    </Button>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="12">
                                    <Button
                                        x:Name="BtnPlayLatestRecording"
                                        Style="{StaticResource OutlinedButtonStyle}"
                                        Click="BtnPlayLatestRecording_Click"
                                        AutomationProperties.Name="Play selected session"
                                        ToolTipService.ToolTip="Play the selected session">
                                        <FontIcon
                                            x:Name="BtnPlayLatestRecordingIcon"
                                            Glyph="&#xE768;"
                                            FontFamily="Segoe Fluent Icons" />
                                    </Button>

                                    <Button
                                        x:Name="BtnSessionOlder"
                                        Style="{StaticResource OutlinedButtonStyle}"
                                        Click="BtnSessionOlder_Click"
                                        AutomationProperties.Name="Go to older session"
                                        ToolTipService.ToolTip="Switch to an older session">
                                        <FontIcon Glyph="&#xE76B;" FontFamily="Segoe Fluent Icons" />
                                    </Button>

                                    <Button
                                        x:Name="BtnSessionNewer"
                                        Style="{StaticResource OutlinedButtonStyle}"
                                        Click="BtnSessionNewer_Click"
                                        AutomationProperties.Name="Go to newer session"
                                        ToolTipService.ToolTip="Switch to a newer session">
                                        <FontIcon Glyph="&#xE76C;" FontFamily="Segoe Fluent Icons" />
                                    </Button>

                                    <Button
                                        x:Name="BtnRetrySpeechToText"
                                        Style="{StaticResource OutlinedButtonStyle}"
                                        Click="BtnRetrySpeechToText_Click"
                                        AutomationProperties.Name="Retry transcription"
                                        ToolTipService.ToolTip="Transcribe the selected session again">
                                        <FontIcon Glyph="&#xE72C;" FontFamily="Segoe Fluent Icons" />
                                    </Button>

                                    <Button
                                        x:Name="BtnUploadSpeechAudio"
                                        Style="{StaticResource OutlinedButtonStyle}"
                                        Click="BtnUploadSpeechAudio_Click"
                                        AutomationProperties.Name="Upload audio for transcription"
                                        ToolTipService.ToolTip="Upload an audio file for transcription">
                                        <FontIcon Glyph="&#xE898;" FontFamily="Segoe Fluent Icons" />
                                    </Button>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Spacing="6">
                                    <TextBlock x:Name="LblSpeechToTextPrompt" Text="Prompt" Style="{StaticResource CaptionTextStyle}" />
                                    <TextBox
                                        x:Name="TxtSpeechToTextPrompt"
                                        PlaceholderText="Optional prompt for context or keywords"
                                        AutomationProperties.LabeledBy="{Binding ElementName=LblSpeechToTextPrompt}"
                                        AutomationProperties.Name="Speech to text prompt"
                                        TextChanged="TxtSpeechToTextPrompt_TextChanged"
                                        TextWrapping="Wrap"
                                        AcceptsReturn="True"
                                        ScrollViewer.VerticalScrollBarVisibility="Auto"
                                        ScrollViewer.VerticalScrollMode="Auto"
                                        ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                        ScrollViewer.HorizontalScrollMode="Auto"
                                        MaxHeight="120" />
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Format Prompt -->
                    <Border x:Name="FormatPromptCard" Style="{StaticResource CardStyle}" Grid.Row="1" Grid.ColumnSpan="2">
                        <StackPanel Spacing="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="4">
                                    <TextBlock Text="LLM Prompts" Style="{StaticResource SectionHeaderTextStyle}" />
                                    <TextBlock Text="Configure multiple prompts for LLM processing." Style="{StaticResource CaptionTextStyle}" />
                                </StackPanel>
                                <Button x:Name="BtnAddPrompt" Grid.Column="1" Content="Add Prompt" Click="BtnAddPrompt_Click" />
                            </Grid>

                            <ListView x:Name="LstPrompts" SelectionMode="None" MaxHeight="200">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid ColumnSpacing="12" Padding="0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                                <StackPanel Orientation="Horizontal" Spacing="8">
                                                    <TextBlock Text="{Binding Hotkey}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                                    <TextBlock Text="(Auto-Run)" FontSize="12" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" AutomationProperties.HelpText="{StaticResource Help.Main.AutoRun}" Visibility="{Binding AutoRun, Converter={StaticResource BoolToVisConverter}}" />
                                                </StackPanel>
                                                <TextBlock Text="{Binding ModelName}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                            </StackPanel>

                                            <Button Grid.Column="1" Content="Run" Click="BtnRunPrompt_Click" Tag="{Binding}" Style="{StaticResource OutlinedButtonStyle}" />
                                            <Button Grid.Column="2" Content="Edit" Click="BtnEditPrompt_Click" Tag="{Binding}" />
                                            <Button Grid.Column="3" Content="Delete" Click="BtnDeletePrompt_Click" Tag="{Binding}" />
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </Border>

                    <!-- Transcripts -->
                    <Border x:Name="TranscriptsCard" Style="{StaticResource CardStyle}" Grid.Row="2" Grid.ColumnSpan="2">
                        <Grid RowSpacing="12" ColumnSpacing="12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Raw Transcript -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Raw Transcript" Style="{StaticResource CaptionTextStyle}" VerticalAlignment="Top" Margin="0,6,0,0" />
                            <TextBox Grid.Row="0" Grid.Column="1"
                                x:Name="TxtRawTranscript"
                                TextWrapping="Wrap"
                                AcceptsReturn="True"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                ScrollViewer.VerticalScrollMode="Auto"
                                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                ScrollViewer.HorizontalScrollMode="Auto"
                                AutomationProperties.Name="Raw transcript"
                                ToolTipService.ToolTip="Raw transcript generated from speech"
                                TextChanged="TxtSpeechToText_TextChanged" />
                            <StackPanel Grid.Row="0" Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Top">
                                <Button
                                    x:Name="BtnFormatTranscript"
                                    Style="{StaticResource AccentButtonStyle}"
                                    Click="BtnFormatTranscript_Click"
                                    AccessKey="F"
                                    AutomationProperties.Name="Format with rules"
                                    AutomationProperties.HelpText="{StaticResource Help.Main.FormatWithRules}"
                                    ToolTipService.ToolTip="Apply your local find-and-replace formatting rules to the transcript (no LLM).">
                                    <FontIcon Glyph="&#xE90F;" FontFamily="Segoe Fluent Icons" />
                                </Button>
                                <Button
                                    x:Name="BtnProcessLlm"
                                    Style="{StaticResource OutlinedButtonStyle}"
                                    Click="BtnProcessLlm_Click"
                                    AccessKey="L"
                                    AutomationProperties.Name="Process with LLM"
                                    AutomationProperties.HelpText="{StaticResource Help.Main.ProcessWithLlm}"
                                    ToolTipService.ToolTip="Send the raw transcript through the configured language model for formatting.">
                                    <FontIcon Glyph="&#xE781;" FontFamily="Segoe Fluent Icons" />
                                </Button>
                            </StackPanel>

                            <!-- Formatted Transcript -->
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Formatted Transcript" Style="{StaticResource CaptionTextStyle}" VerticalAlignment="Top" Margin="0,6,0,0" />
                            <TextBox Grid.Row="1" Grid.Column="1"
                                x:Name="TxtFormatTranscript"
                                TextWrapping="Wrap"
                                AcceptsReturn="True"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                ScrollViewer.VerticalScrollMode="Auto"
                                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                ScrollViewer.HorizontalScrollMode="Auto"
                                AutomationProperties.Name="Formatted transcript"
                                ToolTipService.ToolTip="Formatted transcript ready for sharing" />
                        </Grid>
                    </Border>

                    <!-- Automation -->
                    <Border x:Name="FormattingAutomationCard" Style="{StaticResource CardStyle}" Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2">
                        <StackPanel Spacing="18">
                            <StackPanel Spacing="4">
                                <TextBlock Text="Automation" Style="{StaticResource SectionHeaderTextStyle}" />
                                <TextBlock Text="Control where transcripts are delivered." Style="{StaticResource CaptionTextStyle}" />
                            </StackPanel>

                            <StackPanel Spacing="6">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <TextBlock x:Name="LblThirdPartyInteraction" Text="Third-party interaction" VerticalAlignment="Center" Style="{StaticResource CaptionTextStyle}" />
                                    <controls:InfoHint Text="{StaticResource Help.Main.ThirdPartyInteraction}" VerticalAlignment="Center" />
                                </StackPanel>
                                <ComboBox
                                    x:Name="CmbInsertOption"
                                    PlaceholderText="Select an automation option"
                                    AutomationProperties.LabeledBy="{Binding ElementName=LblThirdPartyInteraction}"
                                    AutomationProperties.Name="Third-party interaction mode"
                                    AutomationProperties.HelpText="{StaticResource Help.Main.ThirdPartyInteraction}"
                                    ToolTipService.ToolTip="Choose how formatted text is inserted"
                                    SelectionChanged="CmbInsertOption_SelectionChanged" />
                                <TextBlock
                                    x:Name="ThirdPartyExplanationText"
                                    TextWrapping="Wrap"
                                    Style="{StaticResource CaptionTextStyle}"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Capture & OCR -->
                    <Border x:Name="CaptureCard" Style="{StaticResource CardStyle}" Grid.Row="5" Grid.Column="0">
                        <StackPanel Spacing="18">
                            <StackPanel Spacing="4">
                                <TextBlock Text="Visual Capture" Style="{StaticResource SectionHeaderTextStyle}" />
                                <TextBlock Text="Quickly capture the screen and extract text with OCR." Style="{StaticResource CaptionTextStyle}" />
                            </StackPanel>

                            <StackPanel Spacing="12">
                                <Button
                                    x:Name="BtnScreenshot"
                                    Style="{StaticResource OutlinedButtonStyle}"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch"
                                    Click="BtnScreenshot_Click"
                                    AccessKey="S"
                                    AutomationProperties.Name="Screenshot to clipboard"
                                    ToolTipService.ToolTip="Copy a screenshot directly to the clipboard">
                                    <Grid ColumnSpacing="12" VerticalAlignment="Stretch">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="32" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <FontIcon
                                            Glyph="&#xE70F;"
                                            FontFamily="Segoe Fluent Icons"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                            <TextBlock Text="Screenshot to clipboard" />
                                            <TextBlock
                                                x:Name="BtnScreenshotHotkey"
                                                Text="Hotkey: SHIFT+ALT+K"
                                                Style="{StaticResource CaptionTextStyle}"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        </StackPanel>
                                    </Grid>
                                </Button>

                                <Button
                                    x:Name="BtnOcrClipboard"
                                    Style="{StaticResource OutlinedButtonStyle}"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch"
                                    Click="BtnOcrClipboard_Click"
                                    AccessKey="C"
                                    AutomationProperties.Name="OCR clipboard"
                                    ToolTipService.ToolTip="Run OCR on an image stored in the clipboard">
                                    <Grid ColumnSpacing="12" VerticalAlignment="Stretch">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="32" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <FontIcon
                                            Glyph="&#xE8F1;"
                                            FontFamily="Segoe Fluent Icons"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                            <TextBlock Text="OCR clipboard" />
                                            <TextBlock
                                                x:Name="BtnOcrClipboardHotkey"
                                                Text="Hotkey: ALT+J"
                                                Style="{StaticResource CaptionTextStyle}"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        </StackPanel>
                                    </Grid>
                                </Button>

                                <Button
                                    x:Name="BtnOcrClipboardLrtb"
                                    Style="{StaticResource OutlinedButtonStyle}"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch"
                                    Click="BtnOcrClipboardLrtb_Click"
                                    AccessKey="D"
                                    AutomationProperties.Name="OCR clipboard left to right"
                                    AutomationProperties.HelpText="{StaticResource Help.Main.OcrReadingOrder}"
                                    ToolTipService.ToolTip="Run OCR on a clipboard image, reading strictly left to right and top to bottom — useful for columns or tables the default order misreads.">
                                    <Grid ColumnSpacing="12" VerticalAlignment="Stretch">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="32" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <FontIcon
                                            Glyph="&#xE8F1;"
                                            FontFamily="Segoe Fluent Icons"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                            <TextBlock Text="OCR clipboard (L→R)" />
                                            <TextBlock
                                                x:Name="BtnOcrClipboardLrtbHotkey"
                                                Text="Hotkey: ALT+K"
                                                Style="{StaticResource CaptionTextStyle}"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        </StackPanel>
                                    </Grid>
                                </Button>

                                <Button
                                    x:Name="BtnScreenshotOcr"
                                    Style="{StaticResource OutlinedButtonStyle}"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch"
                                    Click="BtnScreenshotOcr_Click"
                                    AccessKey="O"
                                    AutomationProperties.Name="Screenshot and OCR"
                                    ToolTipService.ToolTip="Capture a screenshot and extract text automatically">
                                    <Grid ColumnSpacing="12" VerticalAlignment="Stretch">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="32" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <FontIcon
                                            Glyph="&#xE8D2;"
                                            FontFamily="Segoe Fluent Icons"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                            <TextBlock Text="Screenshot &amp; OCR" />
                                            <TextBlock
                                                x:Name="BtnScreenshotOcrHotkey"
                                                Text="Hotkey: SHIFT+ALT+J"
                                                Style="{StaticResource CaptionTextStyle}"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        </StackPanel>
                                    </Grid>
                                </Button>

                                <Button
                                    x:Name="BtnScreenshotOcrLrtb"
                                    Style="{StaticResource OutlinedButtonStyle}"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch"
                                    Click="BtnScreenshotOcrLrtb_Click"
                                    AccessKey="B"
                                    AutomationProperties.Name="Screenshot and OCR left to right"
                                    AutomationProperties.HelpText="{StaticResource Help.Main.OcrReadingOrder}"
                                    ToolTipService.ToolTip="Capture a screenshot and extract text, reading strictly left to right and top to bottom — useful for columns or tables the default order misreads.">
                                    <Grid ColumnSpacing="12" VerticalAlignment="Stretch">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="32" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <FontIcon
                                            Glyph="&#xE8D2;"
                                            FontFamily="Segoe Fluent Icons"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                            <TextBlock Text="Screenshot &amp; OCR (L→R)" />
                                            <TextBlock
                                                x:Name="BtnScreenshotOcrLrtbHotkey"
                                                Text="Hotkey: SHIFT+ALT+E"
                                                Style="{StaticResource CaptionTextStyle}"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        </StackPanel>
                                    </Grid>
                                </Button>

                                <Button
                                    x:Name="BtnOcrDocuments"
                                    Style="{StaticResource OutlinedButtonStyle}"
                                    HorizontalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch"
                                    Click="BtnOcrDocuments_Click"
                                    AccessKey="R"
                                    AutomationProperties.Name="OCR documents"
                                    ToolTipService.ToolTip="Select documents and extract their text in a single batch">
                                    <Grid ColumnSpacing="12" VerticalAlignment="Stretch">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="32" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <FontIcon
                                            Glyph="&#xE898;"
                                            FontFamily="Segoe Fluent Icons"
                                            HorizontalAlignment="Left"
                                            VerticalAlignment="Center" />
                                        <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                            <TextBlock Text="OCR documents" />
                                            <TextBlock
                                                Text="Batch PDFs and images into one result"
                                                Style="{StaticResource CaptionTextStyle}"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        </StackPanel>
                                    </Grid>
                                </Button>

                                <StackPanel x:Name="OcrDocumentsProgressPanel" Spacing="4" Visibility="Collapsed">
                                    <TextBlock
                                        x:Name="OcrDocumentsProgressLabel"
                                        Text="Preparing documents..."
                                        Style="{StaticResource CaptionTextStyle}"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    <ProgressBar
                                        x:Name="OcrDocumentsProgressBar"
                                        Minimum="0"
                                        Maximum="1"
                                        Value="0"
                                        Height="4" />
                                </StackPanel>
                            </StackPanel>

                            <TextBox
                                x:Name="TxtOcr"
                                AcceptsReturn="True"
                                TextWrapping="Wrap"
                                ScrollViewer.VerticalScrollBarVisibility="Auto"
                                ScrollViewer.VerticalScrollMode="Auto"
                                ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                ScrollViewer.HorizontalScrollMode="Auto"
                                AutomationProperties.Name="OCR result"
                                ToolTipService.ToolTip="Text extracted from captured images" />

                            <Button
                                x:Name="BtnDownloadOcrResults"
                                Style="{StaticResource AccentButtonStyle}"
                                HorizontalAlignment="Left"
                                Click="BtnDownloadOcrResults_Click"
                                IsEnabled="False"
                                AutomationProperties.Name="Download OCR result"
                                ToolTipService.ToolTip="Save the OCR output to a text document">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="&#xE118;" FontFamily="Segoe Fluent Icons" />
                                    <TextBlock Text="Download OCR result" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- Voice & Speech -->
                    <Border x:Name="ClipboardCard" Style="{StaticResource CardStyle}" Grid.Row="5" Grid.Column="1">
                        <StackPanel Spacing="18">
                            <StackPanel Spacing="4">
                                <TextBlock Text="Voice &amp; Speech" Style="{StaticResource SectionHeaderTextStyle}" />
                                <TextBlock Text="Speak the current clipboard contents and tune the voice." Style="{StaticResource CaptionTextStyle}" />
                            </StackPanel>

                            <StackPanel Spacing="12">
                                <ComboBox
                                    x:Name="CmbTtsVoice"
                                    Header="Voice"
                                    HorizontalAlignment="Stretch"
                                    SelectionChanged="CmbTtsVoice_SelectionChanged"
                                    AutomationProperties.Name="Text to speech voice"
                                    ToolTipService.ToolTip="Choose a voice for clipboard speech" />

                                <StackPanel Spacing="4">
                                    <TextBlock Text="Rate" />
                                    <Slider
                                        x:Name="SldTtsRate"
                                        Minimum="-10"
                                        Maximum="10"
                                        StepFrequency="1"
                                        TickFrequency="2"
                                        TickPlacement="Outside"
                                        SnapsTo="StepValues"
                                        ValueChanged="SldTtsRate_ValueChanged"
                                        AutomationProperties.Name="Text to speech rate"
                                        ToolTipService.ToolTip="Adjust the speed of clipboard speech" />
                                </StackPanel>

                                <StackPanel Spacing="4">
                                    <TextBlock Text="Volume" />
                                    <Slider
                                        x:Name="SldTtsVolume"
                                        Minimum="0"
                                        Maximum="100"
                                        StepFrequency="1"
                                        TickFrequency="10"
                                        TickPlacement="Outside"
                                        SnapsTo="StepValues"
                                        ValueChanged="SldTtsVolume_ValueChanged"
                                        AutomationProperties.Name="Text to speech volume"
                                        ToolTipService.ToolTip="Adjust speech volume independent of system volume" />
                                </StackPanel>

                                <StackPanel Spacing="8">
                                    <ToggleSwitch
                                        x:Name="ToggleAnnounceReadingTime"
                                        Header="Announce reading time at start"
                                        OffContent="Off"
                                        OnContent="On"
                                        AutomationProperties.Name="Announce reading time at start"
                                        ToolTipService.ToolTip="Speak an estimate of how long the text will take to read before reading begins"
                                        Toggled="ToggleAnnounceReadingTime_Toggled" />
                                    <ToggleSwitch
                                        x:Name="ToggleAnnounceProgress"
                                        Header="Announce progress while reading"
                                        OffContent="Off"
                                        OnContent="On"
                                        AutomationProperties.Name="Announce progress while reading"
                                        ToolTipService.ToolTip="Speak your progress at regular points while reading a long text"
                                        Toggled="ToggleAnnounceProgress_Toggled" />
                                </StackPanel>

                                <StackPanel x:Name="ClipboardActionsPanel" Orientation="Vertical" Spacing="8">
                                    <StackPanel x:Name="ClipboardPrimaryActions" Orientation="Horizontal" Spacing="12">
                                        <Button
                                            x:Name="BtnTextToSpeech"
                                            Style="{StaticResource AccentButtonStyle}"
                                            Click="BtnTextToSpeech_Click"
                                            AccessKey="T"
                                            AutomationProperties.Name="Speak clipboard text"
                                            ToolTipService.ToolTip="Play the clipboard text using text-to-speech">
                                            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                                <FontIcon Glyph="&#xE76E;" FontFamily="Segoe Fluent Icons" />
                                                <TextBlock Text="Speak clipboard" />
                                            </StackPanel>
                                        </Button>

                                        <Button
                                            x:Name="BtnSpeakSelection"
                                            Style="{StaticResource OutlinedButtonStyle}"
                                            Click="BtnSpeakSelection_Click"
                                            AutomationProperties.Name="Speak active selection"
                                            ToolTipService.ToolTip="Copy the current selection from the active app and read it aloud">
                                            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                                <FontIcon Glyph="&#xE8A1;" FontFamily="Segoe Fluent Icons" />
                                                <TextBlock Text="Speak selection" />
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>

                                    <StackPanel x:Name="ClipboardSecondaryActions" Orientation="Horizontal" Spacing="12">
                                        <Button
                                            x:Name="BtnRestartTts"
                                            Style="{StaticResource OutlinedButtonStyle}"
                                            Click="BtnRestartTts_Click"
                                            AutomationProperties.Name="Restart speaking from beginning"
                                            ToolTipService.ToolTip="Speak the clipboard from the beginning, ignoring saved position">
                                            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                                <FontIcon Glyph="&#xE149;" FontFamily="Segoe Fluent Icons" />
                                                <TextBlock Text="Restart" />
                                            </StackPanel>
                                        </Button>

                                        <Button
                                            x:Name="BtnSkipSentenceBack"
                                            Style="{StaticResource OutlinedButtonStyle}"
                                            Click="BtnSkipSentenceBack_Click"
                                            AutomationProperties.Name="Skip sentence backward"
                                            ToolTipService.ToolTip="Jump to the previous sentence">
                                            <FontIcon Glyph="&#xE892;" FontFamily="Segoe Fluent Icons" />
                                        </Button>

                                        <Button
                                            x:Name="BtnSkipSentenceForward"
                                            Style="{StaticResource OutlinedButtonStyle}"
                                            Click="BtnSkipSentenceForward_Click"
                                            AutomationProperties.Name="Skip sentence forward"
                                            ToolTipService.ToolTip="Jump to the next sentence">
                                            <FontIcon Glyph="&#xE893;" FontFamily="Segoe Fluent Icons" />
                                        </Button>

                                        <Button
                                            x:Name="BtnSpeakPosition"
                                            Style="{StaticResource OutlinedButtonStyle}"
                                            Click="BtnSpeakPosition_Click"
                                            AutomationProperties.Name="Speak reading position"
                                            ToolTipService.ToolTip="Speak the current reading position: sentence, percentage, and time remaining">
                                            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                                <FontIcon Glyph="&#xE8B9;" FontFamily="Segoe Fluent Icons" />
                                                <TextBlock Text="Position" />
                                            </StackPanel>
                                        </Button>

                                        <Button
                                            x:Name="BtnSpeakToFile"
                                            Style="{StaticResource OutlinedButtonStyle}"
                                            Click="BtnSpeakToFile_Click"
                                            AutomationProperties.Name="Speak to file"
                                            ToolTipService.ToolTip="Save the clipboard text as a spoken WAV audio file">
                                            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                                                <FontIcon Glyph="&#xE105;" FontFamily="Segoe Fluent Icons" />
                                                <TextBlock Text="Speak to file" />
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                </Grid>

                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup>
                        <VisualState x:Name="Narrow">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="0" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Target="SecondaryColumn.Width" Value="0*" />
                                <Setter Target="MicrophoneCard.(Grid.ColumnSpan)" Value="2" />
                                <Setter Target="SpeechCard.(Grid.Column)" Value="0" />
                                <Setter Target="SpeechCard.(Grid.Row)" Value="1" />
                                <Setter Target="SpeechCard.(Grid.ColumnSpan)" Value="2" />
                                <Setter Target="FormatPromptCard.(Grid.Row)" Value="2" />
                                <Setter Target="FormatPromptCard.(Grid.ColumnSpan)" Value="2" />
                                <Setter Target="TranscriptsCard.(Grid.Row)" Value="3" />
                                <Setter Target="TranscriptsCard.(Grid.ColumnSpan)" Value="2" />
                                <Setter Target="FormattingAutomationCard.(Grid.Row)" Value="4" />
                                <Setter Target="FormattingAutomationCard.(Grid.ColumnSpan)" Value="2" />
                                <Setter Target="CaptureCard.(Grid.Column)" Value="0" />
                                <Setter Target="CaptureCard.(Grid.Row)" Value="5" />
                                <Setter Target="CaptureCard.(Grid.ColumnSpan)" Value="2" />
                                <Setter Target="ClipboardCard.(Grid.Column)" Value="0" />
                                <Setter Target="ClipboardCard.(Grid.Row)" Value="6" />
                                <Setter Target="ClipboardCard.(Grid.ColumnSpan)" Value="2" />
                                <Setter Target="FormattingActionsPanel.Orientation" Value="Vertical" />
                                <Setter Target="ClipboardPrimaryActions.Orientation" Value="Vertical" />
                                <Setter Target="ClipboardSecondaryActions.Orientation" Value="Vertical" />
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState x:Name="Wide">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="900" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Target="SecondaryColumn.Width" Value="*" />
                                <Setter Target="MicrophoneCard.(Grid.ColumnSpan)" Value="1" />
                                <Setter Target="SpeechCard.(Grid.Column)" Value="1" />
                                <Setter Target="SpeechCard.(Grid.Row)" Value="0" />
                                <Setter Target="SpeechCard.(Grid.ColumnSpan)" Value="1" />
                                <Setter Target="FormatPromptCard.(Grid.Row)" Value="1" />
                                <Setter Target="FormatPromptCard.(Grid.ColumnSpan)" Value="2" />
                                <Setter Target="TranscriptsCard.(Grid.Row)" Value="2" />
                                <Setter Target="TranscriptsCard.(Grid.ColumnSpan)" Value="2" />
                                <Setter Target="FormattingAutomationCard.(Grid.Row)" Value="3" />
                                <Setter Target="FormattingAutomationCard.(Grid.ColumnSpan)" Value="2" />
                                <Setter Target="CaptureCard.(Grid.Column)" Value="0" />
                                <Setter Target="CaptureCard.(Grid.ColumnSpan)" Value="1" />
                                <Setter Target="CaptureCard.(Grid.Row)" Value="4" />
                                <Setter Target="ClipboardCard.(Grid.Column)" Value="1" />
                                <Setter Target="ClipboardCard.(Grid.ColumnSpan)" Value="1" />
                                <Setter Target="ClipboardCard.(Grid.Row)" Value="4" />
                                <Setter Target="FormattingActionsPanel.Orientation" Value="Horizontal" />
                                <Setter Target="ClipboardPrimaryActions.Orientation" Value="Horizontal" />
                                <Setter Target="ClipboardSecondaryActions.Orientation" Value="Horizontal" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <Grid
            Grid.Row="0"
            Grid.RowSpan="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">
            <InfoBar
                x:Name="StatusInfoBar"
                Title="Status"
                IsOpen="False"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,24,32,0"
                AutomationProperties.Name="Status notifications"
                AutomationProperties.LiveSetting="Polite"
                AutomationProperties.HelpText="Latest status messages for Mutation workspace" />
        </Grid>
    </Grid>
</Window>
