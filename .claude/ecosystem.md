<!-- Generated by /github-workflow:ecosystem-setup — do not hand-edit. -->
<!-- Regenerate: /github-workflow:ecosystem-setup                      -->

# Ecosystem Tools

> Note: `graphify` is installed but its executable lives in
> `C:/Users/jfbos/AppData/Roaming/Python/Python314/Scripts/` which is not on
> PATH. Either add that directory to PATH, or call it by full path. The
> auto-refresh Stop hook was not installed (the harness blocks writing
> auto-executing hooks without explicit approval) — refresh manually with
> `graphify update .`.

## Graphify — codebase knowledge graph
**What it is:** Builds a searchable map of how the codebase connects —
which files, functions, and modules depend on which — so agents answer
"how does X relate to Y" from the graph instead of guessing from a few
open files. This repo's graph: ~1886 nodes, ~3195 edges, code-only
(docs/images skipped — they need an LLM key and cost tokens).
**Use it:** `graphify update .` after any pull or fresh worktree (rebuilds
from committed cache, ~10s, 0 tokens — note: use `update`, not
`. --update`, to stay code-only with no API key), then
`graphify query "..."`, `graphify path A B`, `graphify explain X`,
`graphify affected X`. Never load `graph.json` into context directly — it
is megabytes of mostly irrelevant detail.
**The workflow uses it:** in the execute **Plan** phase and the
code-review **evaluation** step — prefer a `graphify query` over blind
file search for structure questions. Committed cache lives in
`graphify-out/cache/`; the human-readable report is `docs/GRAPH_REPORT.md`.

## ccusage — Claude Code cost history
**What it is:** Reads your local Claude Code session logs and reports
what you spent, broken down by day, month, session, or project — the
history the built-in `/cost` (current session only) does not show.
**Use it:** `npx ccusage@latest` (also `daily` / `weekly` / `monthly` /
`session` subcommands, and `--instances` to group by project);
`npx ccusage blocks` shows your current 5-hour billing window and a
prediction of when you will hit the rate limit.
**The workflow uses it:** diagnostic — run it yourself to check spend or
pace a long autonomous run against the ~100k-per-session budget. No phase
calls it automatically.

## ecc-agentshield — Claude Code config security scanner
**What it is:** A static scanner that runs fixed rules over your
Claude Code config — CLAUDE.md, settings.json, MCP configs, hooks, and
skills — looking for hardcoded secrets, prompt-injection openings,
overly permissive allowlists, and risky MCP endpoints. No AI, so the
same input always gives the same A–F report.
**Use it:** `npx ecc-agentshield scan` from the repo root. (Baseline at
setup: 4 findings — 2 high "no deny list", 2 medium "no PreToolUse hooks";
both are optional hardening suggestions, not active vulnerabilities.)
**The workflow uses it:** the execute **audit** mode and the code-review
**evaluation** step — run it when the change touches Claude Code config
files (CLAUDE.md, `.claude/`, hooks, skills, MCP config), and fold any
finding into the Security section.

## RTK — terminal output token optimizer
**Not installed.** Selected at setup, but auto-install was unavailable on
this machine (the `curl | sh` installer is sandbox-blocked and `cargo` is
not present). To finish: run
`curl -fsSL https://raw.githubusercontent.com/rtk-ai/rtk/master/install.sh | sh`
in a normal terminal (or `cargo install --git https://github.com/rtk-ai/rtk`),
then `rtk init -g --hook-only` to install the global PreToolUse hook. Do
**not** use `cargo install rtk` — that pulls an unrelated package. Re-run
`/github-workflow:ecosystem-setup` afterward to record it here.
